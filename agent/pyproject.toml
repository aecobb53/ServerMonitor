[project]
name = "server-manager-agent"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "httpx",
    "uvicorn",
    "watchdog",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[project.scripts]
server-manager-agent = "agent.main:app"
